# SSH 终端页工具条与键盘联动进度

## 关联需求

- `docs/requirements/2026-07-17-terminal-toolbar-redesign.md`

## 执行计划

- [x] 确认现有终端输入、硬件按键、键盘避让和剪贴板实现。
- [x] 重构页面间距、终端选择复制与两行等分工具条。
- [x] 实现粘贴、功能键、CTRL/ALT 锁定态与软键盘切换/方向反馈。
- [x] 补充自动化测试，更新 README 和功能记忆。
- [x] 发布 KMM 共享库，清理并重建鸿蒙 HAP。
- [x] 在 API 20+ 模拟器安装，验证键盘展开/收起端点、工具条联动和 SSH 终端画面。

## 验收清单

- [x] 顶部标题栏和终端外部上下间距约为改造前的 50%。
- [ ] 终端内容可长按选择并从系统菜单复制。
- [x] 两行各 10 个按钮等宽等高，并随屏幕宽度平均分配。
- [x] 工具条贴合软键盘移动，展开/收起端点无重叠或闪白。
- [x] 两行按钮顺序、标签与需求一致。
- [x] CTRL/ALT 标签带下划线，状态切换与序列转换有自动化测试覆盖。
- [x] PAST 粘贴、⏏ 切换和展开翻转逻辑已实现；键盘展开端点已截图确认。
- [x] 页面不再显示“复制终端内容”和“发送”按钮。

## 当前结论

- `devecocli` 可用。
- 鸿蒙入口已使用 `KeyboardAvoidMode.RESIZE`；共享 UI 当前使用 `imePadding()`，后续在保持单一避让链路的前提下调整工具条布局。
- `:composeUI:compileKotlinOhosX64` 与 `:composeUI:testDebugUnitTest` 已通过。
- `:composeUI:allTests` 的 iOS Simulator 链接受本机既有 `_Kotlin_KonanStartStub` 缺失影响失败，与本次代码无关；Android common 单测已单独通过。
- 已在 HarmonyOS 7.0.0(26.0.0) Beta1、1256×2760 模拟器完成 SSH 实连截图：终端页双行工具条、键盘收起和展开布局均正常，`HOME` 等标签保持单行。
- 最终正式版本已再次发布共享库、clean 构建并覆盖安装；启动后回到服务器列表，证明临时验收入口未进入最终 HAP。
- 设备自动输入无法命中 Compose 自绘控件，终端长按复制、PAST 实际剪贴板内容及连续 5 次动画仍保留为手工触控复验项。
